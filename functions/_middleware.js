function isAdmin(request){return (request.headers.get('cookie')||'').includes('cq_session=ok')}
export async function onRequest(context){const path=new URL(context.request.url).pathname;if(path.startsWith('/api')||path.startsWith('/adm')||path.startsWith('/src')||path.startsWith('/config')||path.includes('.'))return context.next();if(context.env.SITE_CONFIG){const c=await context.env.SITE_CONFIG.get('site-config','json');if(c?.site?.maintenance?.enabled&&!isAdmin(context.request)){return new Response('<!doctype html><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><body style="margin:0;min-height:100vh;display:grid;place-items:center;background:#02111F;color:#FFFAF6;font-family:system-ui"><main style="max-width:560px;padding:32px"><h1>Voltamos em breve</h1><p>Site em manutenção.</p></main></body>',{status:503,headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}})}}return context.next()}
